import React from "react";

const Rules = () => {
  return (
    <div className="flex justify-center">
      <button className="border-2 font-bold tracking-widest uppercase w-40 py-3 rounded-lg">
        Rules
      </button>
    </div>
  );
};

export default Rules;
